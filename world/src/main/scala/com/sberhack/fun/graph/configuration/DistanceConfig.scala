package com.sberhack.fun.graph.configuration

private[configuration] case class DistanceConfig(
                                                  minDistance: Double,
                                                  maxDistance: Double,
                                                  precision: Int
                                                )
