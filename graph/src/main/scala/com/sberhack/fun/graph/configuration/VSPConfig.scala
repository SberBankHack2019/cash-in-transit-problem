package com.sberhack.fun.graph.configuration

private[configuration] case class VSPConfig(
                                             minValue: Long,
                                             maxValue: Long,
                                             minPickupTime: Long,
                                             maxPickupTime: Long
                                           )