package com.sberhack.fun.graph.configuration

private[configuration] case class VertexConfig(
                                                vsp: VSPConfig
                                              )
