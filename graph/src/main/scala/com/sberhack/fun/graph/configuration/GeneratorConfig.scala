package com.sberhack.fun.graph.configuration

private[configuration] case class GeneratorConfig(
                                                   random: RandomConfig
                                                 )
